# Analysis engine. Pinned to the minor versions this was verified against on
# Python 3.14; nothing here is exotic and older minors will generally work.
numpy>=2.0
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
cvxpy>=1.5            # the synthetic-control convex programme
pytest>=8.0

# Notebooks
matplotlib>=3.8
nbformat>=5.10
nbclient>=0.10
ipykernel>=6.29

# Geospatial. Only needed once real polygons or rasters are connected - the
# tabular adapters run without them, and utils/geo.py raises a clear error at the
# point of use rather than failing at import.
#   pyproj>=3.6
#   shapely>=2.0
#   geopandas>=1.0
#   rasterio>=1.3
#   rioxarray>=0.15

# Ex-ante risk model (Stage 20, not yet built)
#   xgboost>=2.0

# API layer (Milestone 6, not yet built)
#   fastapi>=0.115
#   uvicorn>=0.32
#   pydantic>=2.9
